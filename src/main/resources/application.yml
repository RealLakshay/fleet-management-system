spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/fleet_db
    username: fleet_user
    password: fleet_pass
  jpa:
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
    hibernate:
      ddl-auto: update
    show-sql: false

springfox:
  enabled: true
